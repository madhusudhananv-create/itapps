import { CssBatchMonthlyModel } from './css-batch-monthly-model';

describe('CssBatchMonthlyModel', () => {
  it('should create an instance', () => {
    expect(new CssBatchMonthlyModel()).toBeTruthy();
  });
});
